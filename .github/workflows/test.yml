name: Testing CI

on:
  pull_request:
  push:
    paths:
      - .github/workflows/*.yml

env:
  WASM_SOURCES_PATH: './test/examples'
  WASM_BINARIES_PATH: './test/examples'
  WASM_BINARIES_PATH_WIN: '.\test\examples'
  WASM_MODULE_NAME: 'example'

jobs:
  build_wasm_binary--rust:
    runs-on: ubuntu-latest
    steps:
      - name: Install rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal

      - name: Get 'wasm-pack' tool from cache ...
        uses: actions/cache@v3
        id: cache-wasm-pack
        with:
          path: ~/.cargo/bin/wasm-pack
          key: ${{ runner.os }}-wasm-pack

      - name: ... or install and cache 'wasm-pack' tool
        if: ${{ steps.cache-wasm-pack.outputs.cache-hit != 'true' }}
        run: cargo install wasm-pack

      - uses: actions/checkout@v3
      - run: |
          cd ${{ env.WASM_SOURCES_PATH }}/Rust
          wasm-pack build --target nodejs
      - uses: actions/upload-artifact@v3
        with:
          name: wasm_binary--rust
          path: ${{ env.WASM_SOURCES_PATH }}/Rust/target/wasm32-unknown-unknown/release/${{ env.WASM_MODULE_NAME }}.wasm

  lint_and_test:
    needs:
      - build_wasm_binary--rust

    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        node:
          - 16
          - 18
          - 19
          - 20
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest

    steps:
      - uses: actions/checkout@v3

      - name: Download WASM binaries
        uses: actions/download-artifact@v3

      - name: Move binaries to proper directory
        if: ${{ matrix.os != 'windows-latest' }}
        run: |
          mv wasm_binary--rust/${{ env.WASM_MODULE_NAME }}.wasm ${{ env.WASM_BINARIES_PATH}}
          rm -r wasm_binary--rust

      - name: Move binaries to proper directory (Windows)
        if: ${{ matrix.os == 'windows-latest' }}
        run: |
          Move-Item -Path wasm_binary--rust\${{ env.WASM_MODULE_NAME }}.wasm -Destination ${{ env.WASM_BINARIES_PATH_WIN }} -Force
          Remove-Item -Path wasm_binary--rust -Recurse

      - name: Use Node.js ${{ matrix.node }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node }}
      - uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - run: npm ci
      - run: npm test
